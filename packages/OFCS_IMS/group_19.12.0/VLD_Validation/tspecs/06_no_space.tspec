#-*- coding: utf-8 -*-

write_report_msg ("*** VLD_Validation 강화 테스트 시나리오 (Mandarory필드)")

#///////////////////////////////////////////////////////////////////////////////
# DB XML 설정 및 확인 
write_report_msg("1. Mandarory 필드 check 조건 확인")
write_report_msg(" - ims_charging_Identifier 필드는 공백 허용안됨")

write_report_msg(" - VA 에서 체크되도록 설정한다")
set_xml_db("T_DATA_FORMAT", 
           {"VALUE_CHECK_TYPE":"2","ALLOW_SPACE":"N"}, 
           {"SERVICE_ID":'000005', "DATA_NAME":"IMS_META_VLD", "STRUCTURE_CD":"1", "FIELD_NAME":"ims_charging_Identifier"})

#///////////////////////////////////////////////////////////////////////////////
write_report_msg("2. Sample 데이터를 확인한다.")
write_report_msg(" - 설정 된 필드에 값이 없이 공백인 Sample 및 정상 판정 데이터가 같이 존재함")

# sample CDR 의 특정 필드 값을 logging. ov 명령 수행의 앞 5 라인만 출력
write_report_msg("오류 데이타 (모두 Space)")
run_shell_cmd("ov -f IMS_ARC  ${TEST_DATA_DIR}/sample_cdr/02.NotSpace/tas01_20150715_124957_000102440.arc | grep IMS-Charging-Identifier | head -5")

#///////////////////////////////////////////////////////////////////////////////
write_report_msg("3. VA log file, OUTPUT IPMD 내 directory 전부 삭제한다")
remove_app_file_log("VA*.${CUR_YYYYMMDD}")
remove_all_ipmd_output_files() #ex) /OUTPUT/IMS/IPMD 내 폴더안 file삭제
remove_all_err_output_files() #ex) /OUTPUT/IMS/ERR 내 폴더안 file삭제

#///////////////////////////////////////////////////////////////////////////////
# VA 프로세스를 재초기화 cli 명령 수행
# INIT-PRC:OFCS:OFCS2:IMS:VA
write_report_msg("5. VA 프로세스를 초기화 한다")
run_cli_cmd("INIT-PRC:${PACKAGE_NAME}:${SYSTEM_NAME}:${SERVICE_NAME}:VA") 

#///////////////////////////////////////////////////////////////////////////////
# XXX simul 전송 -> IMS -> DIA_SIM XXX
write_report_msg("6. simul 전송한다")
# tas01.ini 파일 내용중 ACR_FILE_PATH=${TEST_DATA_DIR}/sample_cdr/02.NotSpace 는 실제 경로로 치환된다.
send_simul_dm ("${TEST_DATA_DIR}/client_list/02.NotSpace/client.list", "${TEST_DATA_DIR}/raws/02.NotSpace/tas01.ini") 

#///////////////////////////////////////////////////////////////////////////////
write_report_msg("7. validation 체크로 발생한 alarm(21119003) 을 VA 로그에서 확인한다")
assert_file_grep("ALARM CODE : [21119003]", "${LOG_BASE_PATH}/${SERVICE_NAME}/VA*.${CUR_YYYYMMDD}", 300) # VA01_1.20200727

#///////////////////////////////////////////////////////////////////////////////
# /OUTPUT/IMS/ERR
write_report_msg("8. Error 파일 확인")
write_report_msg("error cdr 에서 STVLD003 문자열을 찾는다. ")
#wait_secs(300) # file 생성 여유 
#assert_cmd_output_include_string ("ov -f IMS_META ${OUTPUT_ERR}/* | fgrep '[ofcs_error_code] : STVLD003' | head -5 ", "STVLD003")
assert_poll_cmd_output_include_string ("ov -f IMS_META ${OUTPUT_ERR}/* | fgrep '[ofcs_error_code] : STVLD003' | head -5 ", "STVLD003", 600)

# XXX XXX mes queue , file db clear TEST
run_cli_cmd("STOP-PRC:${PACKAGE_NAME}:${SYSTEM_NAME}:${SERVICE_NAME}") 
wait_secs(10) 
clear_file_db()    
clear_mes_queue_restart_pfnm()
run_cli_cmd("STAR-PRC:${PACKAGE_NAME}:${SYSTEM_NAME}:${SERVICE_NAME}") 
wait_secs(10) 


